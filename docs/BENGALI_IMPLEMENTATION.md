# ভাষা (Bhasa) - সম্পূর্ণ বাংলা বাস্তবায়ন ডকুমেন্টেশন
# Complete Bengali Implementation Documentation

## সূচিপত্র (Table of Contents)

1. [ভাষার বৈশিষ্ট্য (Language Features)](#ভাষার-বৈশিষ্ট্য)
2. [ডেটা টাইপ (Data Types)](#ডেটা-টাইপ)
3. [অবজেক্ট-ওরিয়েন্টেড প্রোগ্রামিং (OOP)](#অবজেক্ট-ওরিয়েন্টেড-প্রোগ্রামিং)
4. [সংরক্ষিত কীওয়ার্ড (Reserved Keywords)](#সংরক্ষিত-কীওয়ার্ড)
5. [বিল্ট-ইন ফাংশন (Built-in Functions)](#বিল্ট-ইন-ফাংশন)
6. [উদাহরণ প্রোগ্রাম (Example Programs)](#উদাহরণ-প্রোগ্রাম)

---

## ভাষার বৈশিষ্ট্য (Language Features)

### ১. ডায়নামিক টাইপিং (Dynamic Typing)
- ভাষা একটি ডায়নামিক টাইপড ল্যাঙ্গুয়েজ
- টাইপ অ্যানোটেশন ঐচ্ছিক (Optional)
- রানটাইমে টাইপ চেকিং হয়

### ২. প্রথম-শ্রেণীর ফাংশন (First-Class Functions)
- ফাংশনকে ভেরিয়েবলে সংরক্ষণ করা যায়
- ফাংশনকে প্যারামিটার হিসেবে পাস করা যায়
- ক্লোজার সাপোর্ট করে

### ৩. অবজেক্ট-ওরিয়েন্টেড প্রোগ্রামিং (OOP)
- ক্লাস এবং অবজেক্ট
- উত্তরাধিকার (Inheritance)
- এনক্যাপসুলেশন (Encapsulation)
- পলিমরফিজম (Polymorphism)

### ৪. মডিউলার সিস্টেম (Module System)
- `অন্তর্ভুক্ত` (import) কীওয়ার্ড দিয়ে মডিউল ইমপোর্ট

---

## ডেটা টাইপ (Data Types)

### মৌলিক টাইপ (Primitive Types)

#### সংখ্যা সম্পর্কিত টাইপ (Numeric Types):

| টাইপ কীওয়ার্ড | বাংলা নাম | বিবরণ | রেঞ্জ |
|--------------|----------|-------|--------|
| `বাইট` | Byte | ক্ষুদ্রতম সংখ্যা | 0-255 |
| `ছোট_সংখ্যা` | Short | ছোট পূর্ণসংখ্যা | -32,768 to 32,767 |
| `পূর্ণসংখ্যা` | Integer | স্ট্যান্ডার্ড পূর্ণসংখ্যা | -2,147,483,648 to 2,147,483,647 |
| `দীর্ঘ_সংখ্যা` | Long | বড় পূর্ণসংখ্যা | -9,223,372,036,854,775,808 to 9,223,372,036,854,775,807 |
| `দশমিক` | Float | দশমিক সংখ্যা | Single precision |
| `দশমিক_দ্বিগুণ` | Double | উচ্চ নির্ভুলতা দশমিক | Double precision |

#### অন্যান্য মৌলিক টাইপ (Other Primitive Types):

| টাইপ কীওয়ার্ড | বাংলা নাম | বিবরণ |
|--------------|----------|-------|
| `অক্ষর` | Character | একক অক্ষর |
| `পাঠ্য` | String | টেক্সট/স্ট্রিং |
| `বুলিয়ান` | Boolean | সত্য/মিথ্যা |

### জটিল টাইপ (Complex Types):

| টাইপ কীওয়ার্ড | বাংলা নাম | বিবরণ |
|--------------|----------|-------|
| `তালিকা` | Array | অ্যারে/তালিকা |
| `ম্যাপ` | Hash/Map | কী-ভ্যালু ম্যাপ |

### টাইপ কাস্টিং (Type Casting)

```bengali
ধরি সংখ্যা = ১০০;

// বিভিন্ন টাইপে রূপান্তর
ধরি বাইট_মান = সংখ্যা হিসাবে বাইট;
ধরি দশমিক_মান = সংখ্যা হিসাবে দশমিক;
ধরি পাঠ্য_মান = সংখ্যা হিসাবে পাঠ্য;
```

---

## অবজেক্ট-ওরিয়েন্টেড প্রোগ্রামিং (OOP)

### ক্লাস সংজ্ঞা (Class Definition)

#### সিনট্যাক্স:
```bengali
শ্রেণী ক্লাসের_নাম {
    // ফিল্ড (Fields)
    সার্বজনীন ফিল্ড_নাম;
    ব্যক্তিগত অন্য_ফিল্ড;

    // কনস্ট্রাক্টর (Constructor)
    সার্বজনীন নির্মাতা(প্যারামিটার১, প্যারামিটার২) {
        এই.ফিল্ড_নাম = প্যারামিটার১;
        এই.অন্য_ফিল্ড = প্যারামিটার২;
    }

    // পদ্ধতি (Methods)
    সার্বজনীন পদ্ধতি মেথডের_নাম() {
        // কোড
    }
}
```

### অ্যাক্সেস মডিফায়ার (Access Modifiers)

| কীওয়ার্ড | বাংলা অর্থ | বিবরণ |
|----------|-----------|-------|
| `সার্বজনীন` | Public | সর্বত্র অ্যাক্সেসযোগ্য |
| `ব্যক্তিগত` | Private | শুধুমাত্র ক্লাসের ভেতরে |
| `সুরক্ষিত` | Protected | ক্লাস এবং সাবক্লাসে |

### অন্যান্য মডিফায়ার (Other Modifiers)

| কীওয়ার্ড | বাংলা অর্থ | বিবরণ |
|----------|-----------|-------|
| `স্থির` | Static | স্ট্যাটিক মেম্বার |
| `চূড়ান্ত` | Final | পরিবর্তনযোগ্য নয় |
| `বিমূর্ত` | Abstract | অ্যাবস্ট্র্যাক্ট ক্লাস/মেথড |
| `পুনর্সংজ্ঞা` | Override | মেথড ওভাররাইড |

### উত্তরাধিকার (Inheritance)

```bengali
শ্রেণী প্রাণী {
    সার্বজনীন নির্মাতা(নাম) {
        এই.নাম = নাম;
    }

    সার্বজনীন পদ্ধতি শব্দ_করো() {
        লেখ("প্রাণী শব্দ করছে");
    }
}

// উত্তরাধিকার ব্যবহার
শ্রেণী কুকুর প্রসারিত প্রাণী {
    সার্বজনীন নির্মাতা(নাম, জাত) {
        উর্ধ্ব(নাম);  // প্যারেন্ট কনস্ট্রাক্টর কল
        এই.জাত = জাত;
    }

    // মেথড ওভাররাইড
    পুনর্সংজ্ঞা সার্বজনীন পদ্ধতি শব্দ_করো() {
        লেখ("ঘেউ ঘেউ!");
    }
}
```

### ইন্টারফেস (Interfaces)

```bengali
চুক্তি চলনশীল {
    পদ্ধতি চলো();
    পদ্ধতি থামো();
}

শ্রেণী গাড়ি বাস্তবায়ন চলনশীল {
    সার্বজনীন পদ্ধতি চলো() {
        লেখ("গাড়ি চলছে");
    }

    সার্বজনীন পদ্ধতি থামো() {
        লেখ("গাড়ি থামছে");
    }
}
```

---

## সংরক্ষিত কীওয়ার্ড (Reserved Keywords)

### নিয়ন্ত্রণ প্রবাহ (Control Flow)

| কীওয়ার্ড | ইংরেজি | ব্যবহার |
|----------|--------|---------|
| `যদি` | if | শর্ত পরীক্ষা |
| `নাহলে` | else | বিকল্প শর্ত |
| `যতক্ষণ` | while | লুপ |
| `পর্যন্ত` | for | লুপ |
| `বিরতি` | break | লুপ থেকে বের হওয়া |
| `চালিয়ে_যাও` | continue | পরবর্তী ইটারেশন |
| `ফেরত` | return | মান ফেরত দেওয়া |

### ভেরিয়েবল এবং ফাংশন (Variables & Functions)

| কীওয়ার্ড | ইংরেজি | ব্যবহার |
|----------|--------|---------|
| `ধরি` | let | ভেরিয়েবল ঘোষণা |
| `ফাংশন` | function | ফাংশন সংজ্ঞা |
| `সত্য` | true | বুলিয়ান সত্য |
| `মিথ্যা` | false | বুলিয়ান মিথ্যা |

### OOP কীওয়ার্ড (OOP Keywords)

| কীওয়ার্ড | ইংরেজি | ব্যবহার |
|----------|--------|---------|
| `শ্রেণী` | class | ক্লাস সংজ্ঞা |
| `নির্মাতা` | constructor | কনস্ট্রাক্টর |
| `পদ্ধতি` | method | মেথড |
| `এই` | this | বর্তমান অবজেক্ট |
| `নতুন` | new | নতুন ইনস্ট্যান্স |
| `প্রসারিত` | extends | উত্তরাধিকার |
| `উর্ধ্ব` | super | প্যারেন্ট ক্লাস |
| `চুক্তি` | interface | ইন্টারফেস |
| `বাস্তবায়ন` | implements | ইন্টারফেস বাস্তবায়ন |

### অন্যান্য (Others)

| কীওয়ার্ড | ইংরেজি | ব্যবহার |
|----------|--------|---------|
| `স্ট্রাক্ট` | struct | স্ট্রাকচার সংজ্ঞা |
| `গণনা` | enum | ইনাম সংজ্ঞা |
| `অন্তর্ভুক্ত` | import | মডিউল ইমপোর্ট |
| `হিসাবে` | as | টাইপ কাস্টিং |

**⚠️ গুরুত্বপূর্ণ নোট**: উপরের সংরক্ষিত কীওয়ার্ডগুলো ভেরিয়েবল বা প্যারামিটারের নাম হিসেবে ব্যবহার করা যাবে না।

---

## বিল্ট-ইন ফাংশন (Built-in Functions)

### I/O ফাংশন (I/O Functions)

```bengali
লেখ(মান)              // প্রিন্ট/আউটপুট
লেখা(মান)             // toString - মানকে স্ট্রিং-এ রূপান্তর
```

### অ্যারে/স্ট্রিং ফাংশন (Array/String Functions)

```bengali
দৈর্ঘ্য(তালিকা/পাঠ্য)    // দৈর্ঘ্য/লেন্থ
প্রথম(তালিকা)            // প্রথম উপাদান
শেষ(তালিকা)             // শেষ উপাদান
বাকি(তালিকা)            // প্রথম বাদে বাকি
যোগ(তালিকা, মান)        // উপাদান যোগ
সাজাও(তালিকা)           // সাজানো/সর্ট
বিভক্ত(পাঠ্য, বিভাজক)   // স্প্লিট
যুক্ত(তালিকা, যোজক)     // জয়েন
```

### হ্যাশ ফাংশন (Hash Functions)

```bengali
চাবি(ম্যাপ)             // সব কী-এর তালিকা
মান_সমূহ(ম্যাপ)         // সব মান-এর তালিকা
```

### টাইপ চেকিং (Type Checking)

```bengali
টাইপ(মান)              // মানের টাইপ রিটার্ন করে
```

### ম্যাথ ফাংশন (Math Functions)

```bengali
শক্তি(ভিত্তি, ঘাত)      // Power/Exponentiation
```

### ফাইল I/O (File I/O)

```bengali
ফাইল_পড়ো(পথ)          // ফাইল পড়া
ফাইল_লেখো(পথ, বিষয়বস্তু) // ফাইল লেখা
ফাইল_আছে(পথ)          // ফাইল আছে কিনা চেক
```

### JSON ফাংশন (JSON Functions)

```bengali
JSON_পার্স(স্ট্রিং)     // JSON পার্স করা
JSON_স্ট্রিং(অবজেক্ট)  // JSON স্ট্রিং-এ রূপান্তর
```

---

## উদাহরণ প্রোগ্রাম (Example Programs)

### ১. সাধারণ Hello World

```bengali
লেখ("হ্যালো, বিশ্ব!");
```

### ২. ভেরিয়েবল এবং গণনা (Variables & Arithmetic)

```bengali
ধরি ক = ১০;
ধরি খ = ২০;
ধরি যোগফল = ক + খ;

লেখ("যোগফল: " + লেখা(যোগফল));  // যোগফল: 30
```

### ৩. ফাংশন (Functions)

```bengali
ফাংশন গুণ(ক, খ) {
    ফেরত ক * খ;
}

ধরি ফলাফল = গুণ(৫, ৬);
লেখ("৫ × ৬ = " + লেখা(ফলাফল));  // ৫ × ৬ = 30
```

### ৪. শর্ত পরীক্ষা (Conditionals)

```bengali
ধরি বয়স = ১৮;

যদি (বয়স >= ১৮) {
    লেখ("প্রাপ্তবয়স্ক");
} নাহলে {
    লেখ("অপ্রাপ্তবয়স্ক");
}
```

### ৫. লুপ (Loops)

```bengali
// For লুপ
পর্যন্ত (ধরি সূচক = ১; সূচক <= ৫; সূচক = সূচক + ১) {
    লেখ(সূচক);
}

// While লুপ
ধরি গণনা = ০;
যতক্ষণ (গণনা < ৫) {
    লেখ(গণনা);
    গণনা = গণনা + ১;
}
```

### ৬. অ্যারে (Arrays)

```bengali
ধরি সংখ্যা = [১, ২, ৩, ৪, ৫];

লেখ("দৈর্ঘ্য: " + লেখা(দৈর্ঘ্য(সংখ্যা)));  // দৈর্ঘ্য: 5
লেখ("প্রথম: " + লেখা(প্রথম(সংখ্যা)));      // প্রথম: 1
লেখ("শেষ: " + লেখা(শেষ(সংখ্যা)));        // শেষ: 5

// লুপ দিয়ে অ্যারে ট্রাভার্স
পর্যন্ত (ধরি সূচক = ০; সূচক < দৈর্ঘ্য(সংখ্যা); সূচক = সূচক + ১) {
    লেখ(সংখ্যা[সূচক]);
}
```

### ৭. হ্যাশ/ম্যাপ (Hash/Map)

```bengali
ধরি ব্যক্তি = {
    "নাম": "রহিম",
    "বয়স": ২৫,
    "শহর": "ঢাকা"
};

লেখ("নাম: " + ব্যক্তি["নাম"]);
লেখ("বয়স: " + লেখা(ব্যক্তি["বয়স"]));
```

### ৮. ক্লাস এবং অবজেক্ট (Classes & Objects)

```bengali
শ্রেণী ব্যক্তি {
    সার্বজনীন নির্মাতা(নাম, বয়স) {
        এই.নাম = নাম;
        এই.বয়স = বয়স;
    }

    সার্বজনীন পদ্ধতি পরিচয়() {
        ফেরত "আমার নাম " + এই.নাম + ", বয়স " + লেখা(এই.বয়স);
    }
}

ধরি ব্যক্তি১ = নতুন ব্যক্তি("করিম", ৩০);
লেখ(ব্যক্তি১.পরিচয়());
```

### ৯. উত্তরাধিকার (Inheritance)

```bengali
শ্রেণী প্রাণী {
    সার্বজনীন নির্মাতা(নাম) {
        এই.নাম = নাম;
    }

    সার্বজনীন পদ্ধতি শব্দ_করো() {
        লেখ("প্রাণী শব্দ করছে");
    }
}

শ্রেণী কুকুর প্রসারিত প্রাণী {
    সার্বজনীন নির্মাতা(নাম, জাত) {
        উর্ধ্ব(নাম);
        এই.জাত = জাত;
    }

    পুনর্সংজ্ঞা সার্বজনীন পদ্ধতি শব্দ_করো() {
        লেখ(এই.নাম + " ঘেউ ঘেউ করছে!");
    }
}

ধরি আমার_কুকুর = নতুন কুকুর("বাদল", "জার্মান শেফার্ড");
আমার_কুকুর.শব্দ_করো();  // বাদল ঘেউ ঘেউ করছে!
```

### ১০. ফিবোনাচি (Recursion Example)

```bengali
ফাংশন ফিবোনাচি(সংখ্যা) {
    যদি (সংখ্যা <= ১) {
        ফেরত সংখ্যা;
    }
    ফেরত ফিবোনাচি(সংখ্যা - ১) + ফিবোনাচি(সংখ্যা - ২);
}

পর্যন্ত (ধরি গণনা = ০; গণনা < ১০; গণনা = গণনা + ১) {
    লেখ("ফিব(" + লেখা(গণনা) + ") = " + লেখা(ফিবোনাচি(গণনা)));
}
```

---

## প্রযুক্তিগত বাস্তবায়ন (Technical Implementation)

### কম্পাইলার পাইপলাইন (Compiler Pipeline)

1. **লেক্সার (Lexer)** - সোর্স কোডকে টোকেনে বিভক্ত করে
2. **পার্সার (Parser)** - AST (Abstract Syntax Tree) তৈরি করে
3. **কম্পাইলার (Compiler)** - AST থেকে বাইটকোড জেনারেট করে
4. **ভার্চুয়াল মেশিন (VM)** - বাইটকোড এক্সিকিউট করে

### বাস্তবায়িত ফিচার (Implemented Features)

✅ সম্পূর্ণ বাংলা কীওয়ার্ড সাপোর্ট
✅ ডায়নামিক টাইপিং
✅ ঐচ্ছিক টাইপ অ্যানোটেশন
✅ প্রথম-শ্রেণীর ফাংশন
✅ ক্লোজার
✅ অ্যারে এবং হ্যাশ
✅ OOP (ক্লাস, উত্তরাধিকার, এনক্যাপসুলেশন)
✅ বিল্ট-ইন ফাংশন লাইব্রেরি
✅ ফাইল I/O
✅ JSON সাপোর্ট
✅ বিটওয়াইজ অপারেশন
✅ মডিউলার ইমপোর্ট সিস্টেম

### ভবিষ্যৎ উন্নতি (Future Enhancements)

- 🔄 সম্পূর্ণ বাংলা এরর মেসেজ
- 🔄 আরও বিল্ট-ইন লাইব্রেরি
- 🔄 স্ট্যান্ডার্ড লাইব্রেরি মডিউল
- 🔄 প্যাকেজ ম্যানেজমেন্ট সিস্টেম

---

## সাহায্য এবং সম্পদ (Help & Resources)

- **GitHub Repository**: [Bhasa Language](https://github.com/Uttam-Mahata/bhasa)
- **Examples**: `examples/` ডিরেক্টরিতে দেখুন
- **Tests**: `tests/` ডিরেক্টরিতে দেখুন

---

**সর্বশেষ আপডেট**: ২০২৫-১১-০৮
