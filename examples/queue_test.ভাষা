// সারি টেস্ট - Queue Data Structure Test

লেখ("=== Queue (সারি) Test ===");
লেখ("");

// Import queue module
অন্তর্ভুক্ত "modules/সারি";

// Create a new queue
ধরি আমার_সারি = নতুন_সারি();
লেখ("1. Created empty queue");
লেখ("Is empty?");
লেখ(খালি_কিনা(আমার_সারি));
লেখ("");

// Enqueue elements
লেখ("2. Enqueuing elements: 10, 20, 30");
আমার_সারি = এনকিউ(আমার_সারি, 10);
আমার_সারি = এনকিউ(আমার_সারি, 20);
আমার_সারি = এনকিউ(আমার_সারি, 30);
লেখ("Queue size:");
লেখ(আকার(আমার_সারি));
লেখ("");

// Peek at front
লেখ("3. Peek at front element:");
লেখ(সামনে(আমার_সারি));
লেখ("");

// Dequeue elements
লেখ("4. Dequeuing elements:");
ধরি ফলাফল = ডিকিউ(আমার_সারি);
আমার_সারি = প্রথম(ফলাফল);
ধরি ডিকিউ_মান = শেষ(ফলাফল);
লেখ("Dequeued:");
লেখ(ডিকিউ_মান);

ফলাফল = ডিকিউ(আমার_সারি);
আমার_সারি = প্রথম(ফলাফল);
ডিকিউ_মান = শেষ(ফলাফল);
লেখ("Dequeued:");
লেখ(ডিকিউ_মান);

লেখ("Remaining size:");
লেখ(আকার(আমার_সারি));
লেখ("");

লেখ("=== Queue test completed! ===");
