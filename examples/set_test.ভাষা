// সেট টেস্ট - Set Data Structure Test

লেখ("=== Set (সেট) Test ===");
লেখ("");

// Import set module
অন্তর্ভুক্ত "modules/সেট";

// Create empty sets
ধরি প্রথম_সেট = নতুন_সেট();
ধরি দ্বিতীয়_সেট = নতুন_সেট();

লেখ("1. Created empty sets");
লেখ("");

// Add elements to set1
লেখ("2. Adding elements to Set1: 1, 2, 3, 2 (duplicate)");
প্রথম_সেট = যোগ_করো(প্রথম_সেট, 1);
প্রথম_সেট = যোগ_করো(প্রথম_সেট, 2);
প্রথম_সেট = যোগ_করো(প্রথম_সেট, 3);
প্রথম_সেট = যোগ_করো(প্রথম_সেট, 2);  // Duplicate, should not be added
লেখ("Set1 size:");
লেখ(আকার(প্রথম_সেট));
লেখ("Set1 contents:");
লেখ(প্রথম_সেট);
লেখ("");

// Add elements to set2
লেখ("3. Adding elements to Set2: 2, 3, 4, 5");
দ্বিতীয়_সেট = যোগ_করো(দ্বিতীয়_সেট, 2);
দ্বিতীয়_সেট = যোগ_করো(দ্বিতীয়_সেট, 3);
দ্বিতীয়_সেট = যোগ_করো(দ্বিতীয়_সেট, 4);
দ্বিতীয়_সেট = যোগ_করো(দ্বিতীয়_সেট, 5);
লেখ("Set2 size:");
লেখ(আকার(দ্বিতীয়_সেট));
লেখ("Set2 contents:");
লেখ(দ্বিতীয়_সেট);
লেখ("");

// Check contains
লেখ("4. Check contains:");
লেখ("Set1 contains 2?");
লেখ(ধারণ_করে(প্রথম_সেট, 2));
লেখ("Set1 contains 5?");
লেখ(ধারণ_করে(প্রথম_সেট, 5));
লেখ("");

// Union
লেখ("5. Union of Set1 and Set2:");
ধরি একত্র = একত্রীকরণ(প্রথম_সেট, দ্বিতীয়_সেট);
লেখ(একত্র);
লেখ("");

// Intersection
লেখ("6. Intersection of Set1 and Set2:");
ধরি সাধারণ = ছেদ(প্রথম_সেট, দ্বিতীয়_সেট);
লেখ(সাধারণ);
লেখ("");

// Difference
লেখ("7. Difference (Set1 - Set2):");
ধরি পার্থক্য_মান = পার্থক্য(প্রথম_সেট, দ্বিতীয়_সেট);
লেখ(পার্থক্য_মান);
লেখ("");

// Remove element
লেখ("8. Removing element 2 from Set1:");
প্রথম_সেট = মুছো(প্রথম_সেট, 2);
লেখ("Set1 after removal:");
লেখ(প্রথম_সেট);
লেখ("");

লেখ("=== Set test completed! ===");
