// অগ্রাধিকার_সারি (Priority Queue) Data Structure Test

অন্তর্ভুক্ত "অগ্রাধিকার_সারি"

লেখ("=== অগ্রাধিকার সারি পরীক্ষা ===")
লেখ("")

// Create priority queue
ধরি সারি = নতুন_অগ্রাধিকার_সারি()
লেখ("নতুন অগ্রাধিকার সারি তৈরি করা হয়েছে")
লেখ("খালি কিনা? " + টাইপ(খালি_কিনা(সারি)))
লেখ("")

// Insert elements with priorities
লেখ("উপাদান যোগ করা হচ্ছে...")
সারি = সন্নিবেশ(সারি, "জরুরি কাজ", 1)
সারি = সন্নিবেশ(সারি, "সাধারণ কাজ", 5)
সারি = সন্নিবেশ(সারি, "উচ্চ অগ্রাধিকার", 2)
সারি = সন্নিবেশ(সারি, "নিম্ন অগ্রাধিকার", 10)
সারি = সন্নিবেশ(সারি, "মধ্যম অগ্রাধিকার", 6)

লেখ("আকার: " + টাইপ(আকার(সারি)))
লেখ("খালি কিনা? " + টাইপ(খালি_কিনা(সারি)))
লেখ("")

// Peek at highest priority
লেখ("সর্বোচ্চ অগ্রাধিকার: " + টাইপ(উঁকি_দাও(সারি)))
লেখ("")

// Dequeue all elements in priority order
লেখ("অগ্রাধিকার অনুযায়ী সবকিছু বের করা হচ্ছে:")
যতক্ষণ (!খালি_কিনা(সারি)) {
    ধরি ফলাফল = বের_করো(সারি)
    সারি = ফলাফল[0]
    ধরি মান = ফলাফল[1]
    লেখ("  - " + টাইপ(মান))
}
লেখ("")

লেখ("আকার এখন: " + টাইপ(আকার(সারি)))
লেখ("খালি কিনা? " + টাইপ(খালি_কিনা(সারি)))
লেখ("")

// Test with numbers
লেখ("সংখ্যা দিয়ে পরীক্ষা:")
ধরি নাম্বার_সারি = নতুন_অগ্রাধিকার_সারি()
নাম্বার_সারি = সন্নিবেশ(নাম্বার_সারি, 100, 3)
নাম্বার_সারি = সন্নিবেশ(নাম্বার_সারি, 200, 1)
নাম্বার_সারি = সন্নিবেশ(নাম্বার_সারি, 300, 2)

যতক্ষণ (!খালি_কিনা(নাম্বার_সারি)) {
    ধরি ফলাফল = বের_করো(নাম্বার_সারি)
    নাম্বার_সারি = ফলাফল[0]
    লেখ("  মান: " + টাইপ(ফলাফল[1]))
}
লেখ("")

লেখ("✅ অগ্রাধিকার সারি পরীক্ষা সম্পন্ন!")
