// লিংকড_তালিকা টেস্ট - LinkedList Test

লেখ("=== LinkedList (লিংকড_তালিকা) Test ===");
লেখ("");

// Import linkedlist module
অন্তর্ভুক্ত "modules/লিংকড_তালিকা";

// Create empty list
ধরি তালিকা = নতুন_তালিকা();
লেখ("1. Created empty list");
লেখ("Is empty?");
লেখ(খালি_কিনা(তালিকা));
লেখ("");

// Insert at beginning
লেখ("2. Inserting at beginning: 30, 20, 10");
তালিকা = প্রথমে_যোগ(তালিকা, 30);
তালিকা = প্রথমে_যোগ(তালিকা, 20);
তালিকা = প্রথমে_যোগ(তালিকা, 10);
লেখ("Length:");
লেখ(দৈর্ঘ্য_বের_করো(তালিকা));
লেখ("");

// Search for values
লেখ("3. Searching for values:");
লেখ("Contains 20?");
লেখ(খুঁজুন(তালিকা, 20));
লেখ("Contains 99?");
লেখ(খুঁজুন(তালিকা, 99));
লেখ("");

// Remove first
লেখ("4. Removing first element:");
ধরি ফলাফল = প্রথম_মুছো(তালিকা);
তালিকা = প্রথম(ফলাফল);
ধরি মুছে_ফেলা = শেষ(ফলাফল);
লেখ("Removed value:");
লেখ(মুছে_ফেলা);
লেখ("New length:");
লেখ(দৈর্ঘ্য_বের_করো(তালিকা));
লেখ("");

লেখ("=== LinkedList test completed! ===");
