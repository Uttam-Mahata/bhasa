// সম্পূর্ণ বাংলা পরীক্ষা - Complete Bengali Test
// সব নতুন ফিচার বাংলা ভেরিয়েবল দিয়ে পরীক্ষা

লেখ("=== সম্পূর্ণ বাংলা পরীক্ষা ===");
লেখ("");

// ১. For লুপ বাংলা ভেরিয়েবল দিয়ে
লেখ("১. For লুপ (পর্যন্ত):");
পর্যন্ত (ধরি সংখ্যা = 1; সংখ্যা <= 3; সংখ্যা = সংখ্যা + 1) {
    লেখ(সংখ্যা);
}
লেখ("");

// ২. Break এবং Continue
লেখ("২. Break এবং Continue:");
ধরি গণনা = 0;
যতক্ষণ (গণনা < 10) {
    গণনা = গণনা + 1;
    যদি (গণনা == 3) {
        চালিয়ে_যাও;
    }
    যদি (গণনা > 5) {
        বিরতি;
    }
    লেখ(গণনা);
}
লেখ("");

// ৩. Array সাজানো
লেখ("৩. Array সাজানো (সাজাও):");
ধরি সংখ্যা_তালিকা = [5, 2, 8, 1, 9];
ধরি সাজানো_তালিকা = সাজাও(সংখ্যা_তালিকা);
লেখ("সাজানোর আগে:");
লেখ(সংখ্যা_তালিকা);
লেখ("সাজানোর পরে:");
লেখ(সাজানো_তালিকা);
লেখ("");

// ৪. File I/O
লেখ("৪. File I/O:");
ধরি ফাইল = "/tmp/বাংলা_টেস্ট.txt";
ধরি লেখা = "এটি একটি বাংলা টেস্ট ফাইল\n";
ফাইল_লেখো(ফাইল, লেখা);
লেখ("ফাইল লেখা হয়েছে");

যদি (ফাইল_আছে(ফাইল)) {
    ধরি পড়া = ফাইল_পড়ো(ফাইল);
    লেখ("ফাইল থেকে পড়া:");
    লেখ(পড়া);
}
লেখ("");

// ৫. JSON
লেখ("৫. JSON:");
ধরি তথ্য = {
    "নাম": "করিম",
    "বয়স": 25
};
ধরি json_আউটপুট = JSON_স্ট্রিং(তথ্য);
লেখ("JSON হিসেবে:");
লেখ(json_আউটপুট);

ধরি পার্স_করা = JSON_পার্স("[1,2,3]");
লেখ("পার্স করা JSON:");
লেখ(পার্স_করা);
লেখ("");

// ৬. Logical operators
লেখ("৬. Logical operators:");
ধরি ক = 10;
ধরি খ = 20;
যদি (ক > 5 && খ < 30) {
    লেখ("উভয় শর্ত সত্য");
}
যদি (ক < 5 || খ > 15) {
    লেখ("অন্তত একটি শর্ত সত্য");
}
লেখ("");

// ৭. String methods
লেখ("৭. String methods:");
ধরি বাক্য = "এটি একটি পরীক্ষা";
ধরি শব্দ = বিভক্ত(বাক্য, " ");
লেখ("বিভক্ত শব্দ:");
লেখ(শব্দ);
ধরি যুক্ত_বাক্য = যুক্ত(শব্দ, "-");
লেখ("যুক্ত বাক্য:");
লেখ(যুক্ত_বাক্য);
লেখ("");

// ৮. Math functions
লেখ("৮. Math functions:");
ধরি ভিত্তি = 2;
ধরি ঘাত = 3;
ধরি ফলাফল = শক্তি(ভিত্তি, ঘাত);
লেখ("২ এর ঘাত ৩:");
লেখ(ফলাফল);
লেখ("");

লেখ("=== সব পরীক্ষা সফল! ===");
