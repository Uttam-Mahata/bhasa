// স্ট্যাক টেস্ট - Stack Data Structure Test

লেখ("=== Stack (স্ট্যাক) Test ===");
লেখ("");

// Import stack module
অন্তর্ভুক্ত "modules/স্ট্যাক";

// Create a new stack
ধরি আমার_স্ট্যাক = নতুন_স্ট্যাক();
লেখ("1. Created empty stack");
লেখ("Is empty?");
লেখ(খালি_কিনা(আমার_স্ট্যাক));
লেখ("");

// Push elements
লেখ("2. Pushing elements: 10, 20, 30");
আমার_স্ট্যাক = পুশ(আমার_স্ট্যাক, 10);
আমার_স্ট্যাক = পুশ(আমার_স্ট্যাক, 20);
আমার_স্ট্যাক = পুশ(আমার_স্ট্যাক, 30);
লেখ("Stack size:");
লেখ(আকার(আমার_স্ট্যাক));
লেখ("");

// Peek
লেখ("3. Peek at top element:");
লেখ(পিক(আমার_স্ট্যাক));
লেখ("");

// Pop elements
লেখ("4. Popping elements:");
ধরি ফলাফল = পপ(আমার_স্ট্যাক);
আমার_স্ট্যাক = প্রথম(ফলাফল);
ধরি পপ_মান = শেষ(ফলাফল);
লেখ("Popped:");
লেখ(পপ_মান);

ফলাফল = পপ(আমার_স্ট্যাক);
আমার_স্ট্যাক = প্রথম(ফলাফল);
পপ_মান = শেষ(ফলাফল);
লেখ("Popped:");
লেখ(পপ_মান);

লেখ("Remaining size:");
লেখ(আকার(আমার_স্ট্যাক));
লেখ("");

লেখ("=== Stack test completed! ===");
