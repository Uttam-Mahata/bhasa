// বুটস্ট্র্যাপ পরীক্ষা - Bootstrap Test
// স্ব-হোস্টিং বুটস্ট্র্যাপ ক্ষমতা পরীক্ষা

অন্তর্ভুক্ত "modules/ভাষা_কম্পাইলার";

লেখ("======================================");
লেখ("  ভাষা স্ব-হোস্টিং বুটস্ট্র্যাপ পরীক্ষা");
লেখ("======================================");
লেখ("");

// বুটস্ট্র্যাপ ধাপ ১: মৌলিক মডিউল কম্পাইল
লেখ("ধাপ ১: মৌলিক মডিউল কম্পাইল করা হচ্ছে...");
লেখ("");

ধরি মডিউল_তালিকা = [
    {"নাম": "টোকেন", "পথ": "modules/টোকেন.ভাষা"},
    {"নাম": "লেক্সার", "পথ": "modules/লেক্সার.ভাষা"},
    {"নাম": "এএসটি", "পথ": "modules/এএসটি.ভাষা"},
    {"নাম": "পার্সার", "পথ": "modules/পার্সার.ভাষা"},
    {"নাম": "প্রতীক টেবিল", "পথ": "modules/প্রতীক_টেবিল.ভাষা"},
    {"নাম": "কোড", "পথ": "modules/কোড.ভাষা"},
    {"নাম": "কম্পাইলার", "পথ": "modules/কম্পাইলার.ভাষা"},
    {"নাম": "মডিউল লোডার", "পথ": "modules/মডিউল_লোডার.ভাষা"},
    {"নাম": "মূল কম্পাইলার", "পথ": "modules/ভাষা_কম্পাইলার.ভাষা"}
];

ধরি সফল_সংখ্যা = ০;
ধরি মোট_সংখ্যা = দৈর্ঘ্য(মডিউল_তালিকা);

পর্যন্ত (ধরি i = ০; i < মোট_সংখ্যা; i = i + ১) {
    ধরি মডিউল = মডিউল_তালিকা[i];
    লেখ("[" + লেখা(i + ১) + "/" + লেখা(মোট_সংখ্যা) + "] " + মডিউল["নাম"] + "...");
    
    // সিমুলেটেড কম্পাইলেশন (বাস্তবে Go VM এটি পরিচালনা করবে)
    ধরি কম্পাইল_সফল = সত্য;  // placeholder
    
    যদি (কম্পাইল_সফল) {
        লেখ("    ✅ সফল");
        সফল_সংখ্যা = সফল_সংখ্যা + ১;
    } নাহলে {
        লেখ("    ❌ ব্যর্থ");
    }
}

লেখ("");
লেখ("======================================");
লেখ("বুটস্ট্র্যাপ ফলাফল:");
লেখ("  সফল মডিউল: " + লেখা(সফল_সংখ্যা) + "/" + লেখা(মোট_সংখ্যা));
লেখ("  সফলতার হার: " + লেখা((সফল_সংখ্যা * ১০০) / মোট_সংখ্যা) + "%");

যদি (সফল_সংখ্যা == মোট_সংখ্যা) {
    লেখ("  স্থিতি: ✅ বুটস্ট্র্যাপ সফল!");
    লেখ("");
    লেখ("🎉 অভিনন্দন! ভাষা কম্পাইলার সফলভাবে স্ব-হোস্ট হয়েছে!");
} নাহলে {
    লেখ("  স্থিতি: ⚠️ আংশিক বুটস্ট্র্যাপ");
}

লেখ("======================================");
লেখ("");

// ধাপ ২: স্ব-কম্পাইলেশন পরীক্ষা
লেখ("ধাপ ২: স্ব-কম্পাইলেশন সক্ষমতা পরীক্ষা...");
লেখ("কম্পাইলার নিজেকে কম্পাইল করতে পারে কি না যাচাই করা হচ্ছে...");
লেখ("");

ধরি স্ব_কম্পাইল_টেস্ট = "ধরি x = ৫; ধরি y = ১০; ধরি z = x + y;";
লেখ("টেস্ট কোড: " + স্ব_কম্পাইল_টেস্ট);

ধরি ফলাফল = কম্পাইল_সোর্স(স্ব_কম্পাইল_টেস্ট);
যদি (ফলাফল["সফল"]) {
    লেখ("✅ স্ব-কম্পাইলেশন সফল!");
} নাহলে {
    লেখ("❌ স্ব-কম্পাইলেশন ব্যর্থ!");
}

লেখ("");
লেখ("======================================");
লেখ("বুটস্ট্র্যাপ পরীক্ষা সম্পন্ন!");
লেখ("======================================");

