ধরি a: পূর্ণসংখ্যা = 42;
ধরি b: দীর্ঘ_সংখ্যা = 1000000;
ধরি c: লেখা = "হ্যালো";
ধরি d: বুলিয়ান = সত্য;

লেখ("Int:", a);
লেখ("Long:", b);
লেখ("String:", c);
লেখ("Boolean:", d);

ধরি যোগ = ফাংশন(x: পূর্ণসংখ্যা, y: পূর্ণসংখ্যা): পূর্ণসংখ্যা {
    ফেরত x + y;
};

লেখ("Sum:", যোগ(5, 3));

লেখ("Type system working!");
