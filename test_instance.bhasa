লেখ("Starting instance test");

শ্রেণী Person {
    সার্বজনীন নাম: পাঠ্য;

    সার্বজনীন নির্মাতা() {
        লেখ("Constructor called");
    }
}

লেখ("Creating instance");
ধরি p = নতুন Person();
লেখ("Instance created successfully");
