// অগ্রাধিকার_সারি - Priority Queue Data Structure
// Simplified array-based implementation

// নতুন_অগ্রাধিকার_সারি - Creates empty priority queue
ধরি নতুন_অগ্রাধিকার_সারি = ফাংশন() {
    ফেরত []
}

// সন্নিবেশ - Inserts element (priority is implicit by position)
ধরি সন্নিবেশ = ফাংশন(সারি, মান, অগ্রাধিকার) {
    ফেরত যোগ(সারি, মান)
}

// বের_করো - Removes and returns first element
ধরি বের_করো = ফাংশন(সারি) {
    যদি (দৈর্ঘ্য(সারি) == 0) {
        ফেরত [[], 0]
    }
    
    ধরি মান = প্রথম(সারি);
    ধরি নতুন = বাকি(সারি);
    ফেরত [নতুন, মান]
}

// উঁকি_দাও - Returns first element
ধরি উঁকি_দাও = ফাংশন(সারি) {
    যদি (দৈর্ঘ্য(সারি) == 0) {
        ফেরত 0
    }
    ফেরত প্রথম(সারি)
}

// খালি_কিনা - Checks if empty
ধরি খালি_কিনা = ফাংশন(সারি) {
    ফেরত দৈর্ঘ্য(সারি) == 0
}

// আকার - Returns size
ধরি আকার = ফাংশন(সারি) {
    ফেরত দৈর্ঘ্য(সারি)
}
