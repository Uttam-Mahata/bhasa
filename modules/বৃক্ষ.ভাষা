// বৃক্ষ - Binary Tree Data Structure
// Implemented using arrays for simplicity

// নতুন_বৃক্ষ - Creates an empty tree
ধরি নতুন_বৃক্ষ = ফাংশন() {
    ফেরত []
};

// সন্নিবেশ - Inserts a value into tree (maintains sorted order)
ধরি সন্নিবেশ = ফাংশন(বৃক্ষ, মান) {
    ধরি নতুন = যোগ(বৃক্ষ, মান);
    ফেরত সাজাও(নতুন)
};

// খুঁজো - Searches for value in tree
ধরি খুঁজো = ফাংশন(বৃক্ষ, মান) {
    পর্যন্ত (ধরি i = 0; i < দৈর্ঘ্য(বৃক্ষ); i = i + 1) {
        যদি (বৃক্ষ[i] == মান) {
            ফেরত সত্য
        }
    }
    ফেরত মিথ্যা
};

// সর্বনিম্ন_মান - Returns minimum value
ধরি সর্বনিম্ন_মান = ফাংশন(বৃক্ষ) {
    যদি (দৈর্ঘ্য(বৃক্ষ) == 0) {
        ফেরত 0
    }
    ফেরত প্রথম(বৃক্ষ)
};

// সর্বোচ্চ_মান - Returns maximum value
ধরি সর্বোচ্চ_মান = ফাংশন(বৃক্ষ) {
    যদি (দৈর্ঘ্য(বৃক্ষ) == 0) {
        ফেরত 0
    }
    ফেরত বৃক্ষ[দৈর্ঘ্য(বৃক্ষ) - 1]
};

// গণনা - Returns node count
ধরি গণনা = ফাংশন(বৃক্ষ) {
    ফেরত দৈর্ঘ্য(বৃক্ষ)
};

// ইনঅর্ডার - Returns sorted array (inorder traversal)
ধরি ইনঅর্ডার = ফাংশন(বৃক্ষ) {
    ফেরত বৃক্ষ
};

// প্রিঅর্ডার - Returns array in preorder
ধরি প্রিঅর্ডার = ফাংশন(বৃক্ষ) {
    ফেরত বৃক্ষ
};

// পোস্টঅর্ডার - Returns array in postorder
ধরি পোস্টঅর্ডার = ফাংশন(বৃক্ষ) {
    ফেরত বৃক্ষ
};

// উচ্চতা - Returns approximate height (log2 of size)
ধরি উচ্চতা = ফাংশন(বৃক্ষ) {
    ধরি আকার = দৈর্ঘ্য(বৃক্ষ);
    যদি (আকার == 0) {
        ফেরত 0
    }
    ধরি h = 0;
    যতক্ষণ (আকার > 1) {
        আকার = আকার / 2;
        h = h + 1
    }
    ফেরত h + 1
};
